# Configuration API Gateway - Toutes les requêtes passent par le port 3000
# L'API Gateway route automatiquement vers les microservices appropriés
VITE_API_GATEWAY_URL=http://localhost:3000

# ============================================
# Architecture des routes via API Gateway
# ============================================
# Frontend (port 5173) --> API Gateway (port 3000) --> Microservices
#
# Routes disponibles:
# - /auth/*       --> User Service (port 3001) via Gateway
# - /users/*      --> User Service (port 3001) via Gateway
# - /events/*     --> EventCatalogService (port 8080) via Gateway
# - /inventory/*  --> TicketInventoryService (port 8082) via Gateway
# - /payments/*   --> PaymentService (port 8083) via Gateway
